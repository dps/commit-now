This project is a small web application allowing users to make a 'commitment' to do something.  
The idea is to create a database of commitments that can be served up to users.
Users will also (in the final version) receive emails encouraging them to keep their commitments.

